# 面试官：你是怎么处理死循环的？
 - 怎么判断的：
  针对死循环，我从重复和过长两个角度设计检测，重复靠指纹检测，过长靠对话步数与token预算限制。

 - 怎么应对的：
  将模型返回数据用stableStringify + sha256 做指纹（工具调用和工具返回进行加密，判断是否重复），设计三级阈值，分别对应警告，强制停工，熔断退出。
  对于重复调用工具场景，设计滑动窗口检测最新最近对话，查看是否有反复调用同一工具且参数相同无进展情况，是否有互相调用乒乓循环情况，由getNoProgressStreak函数实现，如果对话有进展则继续，无进展才熔断。

# 面试官：你是怎么处理API容错的？
 - 什么情况需要重试
  大模型会返回错误，我们需要判断其中的错误，让大模型继续请求
  可重试:408 超时、429 限流、5xx 服务器错误、网络错误(ECONNRESET/ETIMEDOUT等)、流中断
  不可重试:400/401/403 等 4xx,确定性失败,重试无意义
 - 怎么重试的：
 我通过在单次请求中加入isRetryable函数判断是否需要重试
 在isRetryable中检测http状态码，对可以重试的状态码返回true
 加入指数退避（避免同时发请求，延长请求间隔） + 随机抖动（将大量请求的发送错开，放置同一时间过多请求），重复单次请求
 设定maxRetries: 0关掉aisdk内置重试，避免双层重试
 - 超出重试次数后，怎么处理的：
  目前代码里超出就退出报错，降级之类的还没装上

# 在你的Agent系统中，是怎么处理工具的并发问题的？
  - 工具打造时：
  在工具定义时添加isConcurrencySafe属性
  在ToolRegistry中定义独占锁和共享锁
      exclusiveLock（是否有独占锁持有者）
      concurrentCount（共享锁持有数）
      waitQueue（阻塞队列，存所有暂时拿不到锁的工具）
  - 等到AI SDK 触发工具时：
  toAISDKFormat()将工具打包给模型，模型触发execute时读取所调用工具的属性，如果他是可并发的，走共享锁，如果是不可并发，走独占锁。
  当需要调用一个工具时，会将等待队列中将其取出，其他工具把自己的回调推进等待队列挂起。对于共享锁，当exclusiveLock为false时可以被调用，对于独占锁，exclusiveLock为false的同时concurrentCount==0才可被调用。
  调用完当前工具后，会释放对应的锁，并发工具释放时，concurrentCount会--，如果为零则说明共享锁被释放。不可并发的工具释放时，exclusiveLock会变为false，释放独占锁是唤醒队列里的工具
  释放后，所有队列里的工具回调函数释放，开始抢锁

# 面试官：你接入了哪些第三方的工具？怎么接入的？（src/index.ts、src/tools/mcp-client.ts、src/tools/registry.ts）
  - githubMCP：
    1. 先去GitHub拿去GitHub token
    2. （因为 MCP 依赖子进程通信，连接前我做了个 spawn 能力探测）开子进程进行环境测试
    3. 如果环境允许，我们 new 一个 MCP server，开启GitHubMCP的子进程，并把GitHub toekn传给它，失败了就打印日志，在终端可以看到mcp连接失败，但是agent正常启动
    4. 在注册工具时，连接MCP服务器，（通过client.listTools()）获取外部工具，并注册到工具列表，为了区分本地工具和第三方工具，我们统一给GitHub的工具名加了mcp__github__前缀，并按照本地tool的定义进行适配

  - MCP客户端：
    1. 准备启动指令，参数，环境变量，作为mcpclient的构造器参数，拉取子进程，用管道传输数据（这里面我用stdio的方式，原因是stdio 适合本地能力——不用管端口占用、不用在传输层做认证，生命周期天然绑定父进程，不会留孤儿服务，HTTP transport 适合远程/云端服务）
    2. 发送的信息时JSON-RPC 2.0，先发initialize，确认双方协议版本和能力兼容，响应后才允许工具调用（握手完成）
    3. 发送请求时会有多个请求，为了放置响应不按顺序返回，给请求添加自增id，发送过程中同时挂起一个pending 设定15s，超时就
    4. 监听exit事件，子进程异常退出时，立即 reject 所有 pending 请求，方式Promise 还在傻等 15 秒假超时


# 面试官：工具太多大模型选不准，你是怎么处理的？
  - 为什么会选不准：

  - 怎么处理的：(ToolSearch)
