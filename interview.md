# 面试官：你是怎么处理死循环的？
 - 怎么判断的：
判断模型是否重复调用工具，传递的参数是否相同，对话过长迟迟不返回结果
 - 怎么应对的：
对此我进行了检测，将模型返回但数据装入一个数组，先判断对话是否超出预期，如果是重复，则在对话中注入警告，如果达到最大限制侧强制退出。针对调用工具的死循环，通过连续遍历参数和工具，是否重复调用，如果有重复调用就警告，并且为了防止乒乓循环，从history数组中从前到后，从后道歉的查看临近对话是否相同，如果有则警告，直至熔断退出。

# 面试官：你是怎么处理API容错的？
 - 什么情况需要重试
  超时错误
  网络错误
 - 怎么重试的：
  重新对话，设定重新对话次数
  针对网络错误，判单https状态码，针对不同的状态码进行充实
 - 超出重试次数后，怎么处理的：
  目前代码里超出就退出报错，降级之类的还没装上